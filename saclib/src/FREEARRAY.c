/*==========================================================================
<macro>                       FREEARRAY(A)

Free array.

Input
  A : a pointer to an array.

Side effect
  The memory allocated to A is freed.

#define FREEARRAY(A)   free((A))
===========================================================================*/
