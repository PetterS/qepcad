/*===========================================================================
<macro>                       n <- ILENGTH(A)

Integer length.

Input
  A : in Z.

Output
  n : the word length of A.

#define ILENGTH(A)     (((A) < BETA) ? 1 : LENGTH(A))
===========================================================================*/
